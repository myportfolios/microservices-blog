apiVersion : v1
kind: Pod
metadata:
  name: posts
spec:
  containers:
    - name: posts
      image: 23101/posts:0.0.1

# This yaml file is no longer in use as we don't want to create individual pods.
# The kuberenetes Deployment will handle the creation of pods